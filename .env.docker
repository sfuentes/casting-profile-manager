# Docker Environment Configuration
# This file contains environment variables for Docker deployment
# Copy this to .env and customize as needed

# IMPORTANT: Change JWT_SECRET for production!
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# MongoDB Credentials (must match docker-compose.yml)
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=admin123

# Application Settings
NODE_ENV=production
PORT=5000

# Database Connection (automatically set in docker-compose.yml)
# MONGO_URI is set in docker-compose.yml, no need to duplicate here

# JWT Configuration
JWT_EXPIRES_IN=7d
JWT_COOKIE_EXPIRE=7

# Rate Limiting
RATE_LIMIT_WINDOW_MS=600000
RATE_LIMIT_MAX=100

# File Upload
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10
FILE_UPLOAD_PATH=./uploads

# Email Configuration (Optional - for password reset, etc.)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_email@example.com
# SMTP_PASSWORD=your_password
# FROM_EMAIL=noreply@castingmanager.com
# FROM_NAME=Casting Manager

# Platform API Keys (Optional - add as needed)
# FILMMAKERS_CLIENT_ID=
# FILMMAKERS_CLIENT_SECRET=
# CASTING_NETWORK_API_KEY=
# SCHAUSPIELERVIDEOS_API_KEY=
# ETALENTA_API_KEY=
# JOBWORK_CLIENT_ID=
# JOBWORK_CLIENT_SECRET=

# Frontend URL (for CORS in production)
# FRONTEND_URL=https://your-frontend-domain.com
